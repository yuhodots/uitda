extends ../layout.pug

block content
    #form
        form(action="/view/manage/proposal/create" method="post")
            p
                input(type="text" name="title" placeholder="title")
            p
                textarea(name="description" placeholder="description")
            p
                input(type="submit")
